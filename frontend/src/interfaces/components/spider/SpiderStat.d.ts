interface SpiderStatProps {
  stat?: SpiderStat;
}

interface SpiderStatTooltips {
  tasks: string;
  results: string;
  duration: string;
}

interface SpiderStatLabels {
  tasks: string;
  results: string;
  duration: string;
}
